

class Entry():

    def __init__(self, date, body, version, commit_id, release_link):
        self.date = date
        self.body = body
        self.version = version
        self.commit_id = commit_id
        self.release_link = release_link
